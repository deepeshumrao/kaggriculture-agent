# Core agent stack (install when you enable the LLM policy)
google-adk>=0.1.0
google-genai>=0.3.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0

# Utilities
# (numpy is optional; the mock sim and heuristics use only the stdlib)
